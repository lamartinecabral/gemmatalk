{
  "name": "gemmatalk",
  "scripts": {
    "lint": "npx -y @biomejs/biome lint",
    "serve": "npx -y serve",
    "test": "npm run typecheck && npm run lint",
    "typecheck": "tsc"
  },
  "dependencies": {
    "@litert-lm/core": "0.15.0",
    "typescript": "^6.0.3"
  }
}
